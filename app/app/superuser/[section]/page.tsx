import Link from 'next/link';
import { getServiceClient } from '../../../lib/auth';
export const dynamic='force-dynamic';
type Config={title:string;subtitle:string;table:string;columns:string[]};
const configs:Record<string,Config>={
 organizations:{title:'Organizations',subtitle:'Every customer account in PIEify.',table:'accounts',columns:['name','status','created_at']},users:{title:'Users',subtitle:'People attached to PIEify customer accounts.',table:'profiles',columns:['email','role','account_id','created_at']},subscriptions:{title:'Subscriptions & seats',subtitle:'Plans, subscription state and seat allocation.',table:'subscriptions',columns:['plan','status','monthly_value','created_at']},'demo-requests':{title:'Demo requests',subtitle:'Inbound requests from the PIEify site.',table:'demo_requests',columns:['first_name','last_name','company','email','status','created_at']},tickets:{title:'Support',subtitle:'Customer issues currently recorded in PIEify.',table:'support_tickets',columns:['subject','status','priority','requester_name','created_at']},tenders:{title:'Tenders',subtitle:'Tender records flowing through the platform.',table:'bid_sessions',columns:['name','status','created_at']},requirements:{title:'Requirements',subtitle:'Structured tender requirements and evaluation inputs.',table:'requirements',columns:['name','status','created_at']},inventory:{title:'Inventory',subtitle:'Apparatus and inventory records available to PIEify.',table:'inventory_items',columns:['name','status','created_at']},catalogs:{title:'Catalogs',subtitle:'OEM and dealer catalog sources.',table:'catalogs',columns:['name','status','created_at']},relationships:{title:'OEMs & dealers',subtitle:'Business relationships between organizations and suppliers.',table:'business_relationships',columns:['name','status','created_at']},extraction:{title:'Extraction',subtitle:'Document extraction and workflow records.',table:'document_workflows',columns:['name','status','current_step','updated_at']},matching:{title:'Matching',subtitle:'Product and inventory matching records.',table:'match_results',columns:['status','created_at']},scoring:{title:'Scoring',subtitle:'Scoring models and score runs.',table:'score_runs',columns:['status','created_at']},questions:{title:'Questions',subtitle:'Clarification questions generated or recorded for tenders.',table:'clarification_questions',columns:['question','status','created_at']},ai:{title:'AI runs',subtitle:'Agent executions, models and run status.',table:'ai_runs',columns:['run_type','model','status','started_at','completed_at']},automations:{title:'Automations',subtitle:'Automation and workflow activity.',table:'document_workflows',columns:['name','status','current_step','updated_at']},workflows:{title:'Workflows & jobs',subtitle:'Background workflow records and current state.',table:'document_workflows',columns:['name','status','current_step','updated_at']},integrations:{title:'Integrations',subtitle:'Integration synchronization runs.',table:'integration_sync_runs',columns:['direction','entity_type','status','records_read','records_written','records_failed','started_at']},'data-quality':{title:'Data quality',subtitle:'Exceptions requiring attention.',table:'exception_queue_items',columns:['status','severity','created_at']},revenue:{title:'Revenue',subtitle:'Subscription revenue records.',table:'subscriptions',columns:['plan','status','monthly_value','created_at']},usage:{title:'Usage & tokens',subtitle:'Observed AI usage records and token consumption.',table:'ai_run_usage',columns:['provider','model','input_tokens','output_tokens','cached_input_tokens','actual_cost','created_at']},costs:{title:'AI costs',subtitle:'Platform cost ledger records.',table:'platform_cost_ledger',columns:['amount','category','created_at']},deployments:{title:'Deployments',subtitle:'Deployment state is tracked by the hosting platform.',table:'deployment_events',columns:['status','created_at']},security:{title:'Security',subtitle:'Security-relevant platform events.',table:'audit_events',columns:['event_type','entity_type','action','occurred_at']},audit:{title:'Audit log',subtitle:'Immutable record of platform activity exposed to PIEify.',table:'audit_events',columns:['event_type','entity_type','action','occurred_at']},settings:{title:'Platform settings',subtitle:'Configuration records available to the platform.',table:'platform_settings',columns:['key','value','updated_at']}};
const label=(v:string)=>v.replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
const show=(v:unknown)=>v==null||v===''?'—':typeof v==='object'?JSON.stringify(v):String(v);
export default async function SuperuserObjectIndex({params,searchParams}:{params:Promise<{section:string}>;searchParams:Promise<{q?:string;sort?:string;dir?:string;view?:string}>}){
 const {section}=await params; const sp=await searchParams; const config=configs[section]; if(!config)return <div><h1 className="hs-page-title">Not configured</h1></div>;
 const db=await getServiceClient(); const {data,error,count}=await db.from(config.table).select('*',{count:'exact'}).limit(100); const raw=(data||[]) as Record<string,unknown>[]; const q=(sp.q||'').trim().toLowerCase(); const sort=sp.sort&&config.columns.includes(sp.sort)?sp.sort:''; const dir=sp.dir==='desc'?'desc':'asc'; const rows=raw.filter(r=>!q||config.columns.some(c=>show(r[c]).toLowerCase().includes(q))).sort((a,b)=>sort?show(a[sort]).localeCompare(show(b[sort]))*(dir==='asc'?1:-1):0); const keys=raw.length?config.columns.filter(k=>k in raw[0]):config.columns; const qs=(extra:Record<string,string>)=>{const p=new URLSearchParams();if(q)p.set('q',q);if(sort)p.set('sort',sort);if(dir)p.set('dir',dir);if(sp.view)p.set('view',sp.view);Object.entries(extra).forEach(([k,v])=>p.set(k,v));return `?${p.toString()}`};
 return <div><div className="hs-breadcrumb"><Link href="/app/superuser">Platform Home</Link> / {config.title}</div><div className="flex items-end justify-between gap-5"><div><h1 className="hs-page-title">{config.title}</h1><p className="hs-page-sub">{config.subtitle}</p></div><div className="text-right text-xs text-gray-500"><b className="text-gray-800">{count??(error?'—':raw.length)}</b><br/>records</div></div><form className="hs-toolbar"><input name="q" defaultValue={sp.q||''} placeholder="Search records" className="hs-btn" style={{minWidth:240}}/><select name="sort" defaultValue={sort} className="hs-btn"><option value="">Sort by</option>{config.columns.map(c=><option key={c} value={c}>{label(c)}</option>)}</select><select name="dir" defaultValue={dir} className="hs-btn"><option value="asc">Ascending</option><option value="desc">Descending</option></select><button className="hs-btn" type="submit">Apply</button><Link className="hs-btn" href={`/app/superuser/${section}`}>Clear</Link><div className="hs-grow"/><Link className="hs-btn" href={qs({view:'table'})}>Table</Link><Link className="hs-btn" href={qs({view:'cards'})}>Cards</Link></form>{error?<section className="rounded-lg border border-gray-200 bg-white p-6 text-sm"><b>Not configured:</b> {error.message}</section>:sp.view==='cards'?<div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{rows.map((row,i)=><Link href={row.id?`/app/superuser/${section}/${row.id}`:'#'} key={String(row.id??i)} className="rounded-lg border border-gray-200 bg-white p-5 hover:border-blue-300">{keys.slice(0,5).map(k=><div key={k} className="mb-3"><div className="text-[11px] uppercase tracking-wide text-gray-500">{label(k)}</div><div className="mt-1 text-sm text-gray-900">{show(row[k])}</div></div>)}</Link>)}</div>:<section className="overflow-hidden rounded-lg border border-gray-200 bg-white"><div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="border-b border-gray-200 bg-gray-50"><tr>{keys.map(k=><th key={k} className="px-4 py-3 font-semibold text-gray-600">{label(k)}</th>)}</tr></thead><tbody className="divide-y divide-gray-100">{rows.map((row,i)=><tr key={String(row.id??i)} className="hover:bg-gray-50">{keys.map((k,j)=><td key={k} className="px-4 py-3 text-gray-800">{j===0&&row.id?<Link className="font-semibold text-blue-700 hover:underline" href={`/app/superuser/${section}/${row.id}`}>{show(row[k])}</Link>:show(row[k])}</td>)}</tr>)}</tbody></table></div>{!rows.length&&<div className="px-5 py-10 text-sm text-gray-500">No matching records in production.</div>}</section>}</div>;
}
